/target
/dist
**/*.rs.bk
.env
*.pdb

# node / playwright
/node_modules
/test-results
/playwright-report
/.playwright

# local review artifacts (root copy is generated; site/registry.json is committed for deploy)
/registry.json

# control-panel embeds per-OS connector binaries + a registry copy at build time
/crates/control-panel/resources/*.exe
/crates/control-panel/resources/*.bin
/crates/control-panel/resources/registry.json
/crates/control-panel/target

# internal working docs (specs/plans) - not for the public repo
/docs/superpowers

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
