{
  "name": "claude-chat-mcp-site",
  "version": "0.1.0",
  "private": true,
  "description": "Local catalog site + Playwright evaluation harness for Claude Chat MCP connectors.",
  "scripts": {
    "serve": "node scripts/static-server.mjs site 4321",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "@playwright/test": "^1.49.0",
    "wrangler": "^4.103.0"
  }
}
